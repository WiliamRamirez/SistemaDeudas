import { createMuiTheme } from '@material-ui/core';

const themeMui = createMuiTheme({
    palette: {
        primary: {
            light: '#9162e4',
            main: '#5e35b1',
            dark: '#280680',
            contrastText: '#ffffff',
        },
    },
});

export default themeMui;
